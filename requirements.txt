# SAR-Optical Reconstruction Project
# Paper 2: Monsoon Gap Filling via SAR + Optical Fusion
# Install PyTorch separately first (see note below)

# -------------------------------------------------------
# STEP 1 — Install PyTorch with correct CUDA version first
# For NVIDIA GPU (CUDA 12.1):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# For Mac (Apple Silicon MPS):
#   pip install torch torchvision
# For CPU only:
#   pip install torch torchvision
# -------------------------------------------------------

# Core deep learning
torchvision

# Geospatial / raster (rasterio bundles GDAL internally — no separate GDAL needed)
rasterio>=1.3.0
shapely>=2.0.0
pyproj>=3.4.0
geopandas>=0.13.0

# Data handling
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# Image processing & metrics
scikit-image>=0.21.0
opencv-python>=4.8.0
Pillow>=10.0.0

# Machine learning utilities
scikit-learn>=1.3.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Progress & utilities
tqdm>=4.65.0
einops>=0.6.0

# Logging & experiment tracking (optional but recommended)
tensorboard>=2.13.0

# Notebook support (optional)
ipykernel>=6.0.0
jupyterlab>=4.0.0